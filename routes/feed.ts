import {Router} from "express";
// import {addFeed,getFeeds} from '../controllers/feeds';

const router = Router();

// router.get('/', getFeeds);
// router.post('/', addFeed);

export default router;